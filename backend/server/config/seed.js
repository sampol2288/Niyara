/**
 * Seeding has been explicitly disabled.
 * The application connects directly to MongoDB Atlas without inserting any automatic seed data.
 */
export const seedDatabase = async () => {
  // No-op: Data seeding disabled per explicit instructions.
  return;
};
