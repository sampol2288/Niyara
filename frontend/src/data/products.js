// Products data is now fetched from MongoDB via the API.
// These empty arrays are kept as fallbacks only.
export const PRODUCTS = [];

export const MOCK_REVIEWS = [];

export const MOCK_ORDERS = [];
